import { createMuiTheme } from "@material-ui/core/styles";

export default createMuiTheme({
	palette: {
		primary: {
			// main: deepOrange[300],
			main: "#bb586c",
		},
		secondary: {
			main: "#e5e5e5",
		},
		error: {
			main: "#ff604f",
		},
	},
});
